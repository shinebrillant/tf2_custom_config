// binds
// ===

// move
// null_cancelling_movement
// w, s, a, d
// forward, back, moveright, moveleft

// null_cancelling_movement
// exec user/mouse_prim_sec_melee.cfg
// attack, attack 2
// mouse1, mouse2, mouse3
// class attack set

// Game Key binds
bind CAPSLOCK +quickswitch
bind SHIFT +duck
bind CTRL +reload
bind r +use_action_slot_item
bind SEMICOLON say_party

unbind mouse5
bind 4 slot4
bind q lastinv
alias +c_attack"+attack;cmd spec_next"
alias -c_attack -attack
bind mouse1 +c_attack
alias +c_attack2"+attack2;cmd spec_prev"
alias -c_attack2 -attack2
bind mouse2 +c_attack2
crosshair_color_default

// ehco
echo "==="
echo "binds.cfg is laoding"
echo "==="
