// demoman
// ===

// leave at the top
exec reset

// null_cancelling_movement
// exec overrides/mouse_prim_sec_melee.cfg

// null_cancelling_movement
exec overrides/null_cancelling_movement.cfg

// tabgraph
exec overrides/tabgraph.cfg

// binds
exec overrides/binds.cfg

// set
exec overrides/setting.cfg

// null_cancelling_movement
exec overrides/mouse_prim_sec_melee.cfg
bind "MOUSE1" "+scattergun"
bind "MOUSE2" "+pistol"
bind "E" "+melee"

alias +specific_skills "+attack2"
alias -specific_skills "-attack2"
bind "Q" "+specific_skills"

// ---
// ehco
// ---
echo "==="
echo "demoman.cfg is loading"
echo "==="
